# What is Python?
=> Python is a High Level Language which uses interpreter to convert the high level code into Machine Code.


# Why learn to Python?
=> We should learn Python because:
1. Python is simple and easy to learn.
2. It is free and open source.
3. It is a High Level Language which is used for Data Science, Data Analytics, Machine Learning, Artificial Intelligence and also Web Development.
4. It was developed by Guido van Rossum.
5. It is portable i.e. we can write Python code on one platform say Windows and same code can be executed on any other platforms like Linux, macOS, etc.


# How to download Python?
=> Go to browser and:
1. Search for Python for Windows.
2. You will see Python Download url:- python.org
3. Visit the url and you will see download button.
4. Download from there and install.

NOTE:- For Windows, may sure that when installation set-up appears you check both boxes for admin access and path set-up.


# Once installation is done you can verify it in the. terminal by using command: python3 --version
If you see the Python version displayed then it is successfully installed and if not then it did not install properly and you need to re-install it.


# Now you can go to VS Code and install Python extension:- Python language support with extension access points for IntelliSense (Pylance), Debugging (Python Debugger), linting, formatting, refactoring, unit tests, and more.


# Once installation is complete, you can create a folder and open it in VS Code and then create a file with with any name and the extension of the file should be ".py" , e.g. FirstProgram.py 

After creating file you can try a basic output command for Python i.e. to Print Hello World:
print("Hello World")

After this you can save changes in file. Also note that "Prettier" formatter will not work with Python file, and you need to install separate extension like "Black" or "Autopep8" to format Python files.


# To run the code you can click on Run button on the top right corner of VS Code, and select "Run Python File", or in terminal you can type "python3 fileName".

=> Printing output in Python:-
print("Hello World")
# Here print() method is used to print output.


# Python Character Set
1. Letters: A to Z, a to z 
2. Digits: 0 to 9 
3. Special Symbols: + - * / etc 
4. Whitespaces: Blank Spaces, tab, carriage return, newline, formfeed
5. Other Characters: Python can process all ASCII and Unicode characters as part of data or literals.

# Printing in different line:
print("My name is Om Prakash.")
print("I am 26 years old.")

# Printing in same line:
print("My name is Om Prakash.", "I am 26 years old.")

# Printing numbers in various formats:
print(26)
print(5 + 7)
print(5 - 7)
print(5 * 7)
print(7 / 5)


# Variables in Python:
=> A variable in a name given to a memory location in a program. e.g.

name = "Om Prakash"  # This variable has String value.
age = 26  # This variable has Integer value.
price = 25.99  # This variable has Float value.

print(name)
print(age)
print(name, age, price)
print("My name is", name, ".", "I am", age, "years old.")


# Assigning one variable value to other variable:
age2 = age
print(age2)


# Rules for Identifiers:
=> i.e. Rules for naming variables, functions, classes, etc.
1. Identifiers can be combination of uppercase and lowercase letters, digits, or an underscore(_). So, myVariable, variable_1, variable_for_print, all are valid Python identifiers.
2. An Identifier can not start with digit. So, while "variable1" is valid, "1variable" is not valid.
3. We can't use special symbols like !, #, @, %, $, etc in our Identifier.
4. Identifier can be of any length.

=> Make sure while naming Identifiers the name should be simple, short and meaningful.

=> Another important thing about variable is that you can check the data type of the variable by using "type()" method. e.g.

print(type(name))
print(type(age))
print(type(price))


# Data types in Python: There are various data types in Python but we mainly study followings:
# 1. Primitive data types: These are data types which stores only one value in a variable. It uses a small fixed amount of memory.

# i. Integers: We can have -ve, zero, and +ve values in Integer data type. e.g.

quantity = 240
print(quantity)

volume = 0
print(volume)

profit = -12
print(profit)

# ii. String: Anything written in "" or '' or '''''' is treated as String data type but we will mostly use "". e.g.

quality = 'satisfactory'
print(quality)

status = "OK Tested"
print(status)

release = '''29th July 2026'''
print(release)

release2 = """21st December 2026"""
print(release2)

# iii. Float: All decimal values are treated as Float data type. e.g.

area = 12.55
print(area)

quantity = 0.0
print(quantity)

stockPrice = -1.2
print(stockPrice)

# iv. Boolean: It holds only two values i.e. either True or "False", make sure while writing True or False use capital T and capital F or else it will throw error. e.g.

isAvailable = True
print(isAvailable)

isValid = False
print(isValid)

# v. None: This is assigned to those variables where we don't want to store any value currently. e.g.

none = None
print(none)


# 2. Non-primitive data type: Also known as Derived data type. These can hold multiple values in a single variable. It uses variable amount of memory.
# i. list: An ordered, mutable sequence of items that allows duplicate elements and is written in []. This is equivalent to writing and array in JS. e.g.

list = [1, 2, "apple"]
print(list)

# ii. tuple: An ordered, immutable sequence of items that allows duplicate elements and is written in (). e.g.

tuple = (10, 20, 30)
print(tuple)

# iii. dict: This stands for Dictionary. It is an un-unordered, mutable collection of key-value pairs and is written in {}. This is equivalent to writing JSON in JS. e.g.

dict = {"name": "Alice", "age": 25}
print(dict)

# iv. set: An unordered collection of unique, mutable items that does not allow duplicates. It is also written in {}. e.g.

set = {1, 2, 3}
print(set)

=> Even Classes and Objects are also considered as non-primitive data type in Python.


# Keywords: These are reserved words in Python. These words should not be used as Identifiers. Some reserved words are given below:
and, as, assert, break, class, continue, def, del, elif, else, expect, finally, False, for, from, global, if, import, in, is, lambda, nonlocal, None, not, or, pass, raise, return, True, try, with, while, yield, etc.

=> Also one important thing to remember is that there are some languages which are case-sensitive like Java, C++ and some are case-insensitive like SQL, HTML. Python is case-sensitive, meaning if you write a variable name as 'cost' then it is not equal to writing 'Cost'. These two variables will be treated differently.


# Let's Print the sum of two Numbers:
num1 = 2
num2 = 5

sum = num1 + num2
print(sum)


# Comments in Python: Comments are those parts in the program which are not executed. We write comments to explain our code in normal English. We can write comments in two ways.

1. Single Line Comment: For single line comment we use # symbol. e.g.

# This is a Single Line Comment.
sum = num1 + num2 # This is logic for adding two numbers

2. Multi-Line Comment: If you want to comment continuously in more than one line then you can use """""". I know we have discussed it as String but if you don't store this in any variable then it will be treated as comment as Garbage collector will take it when executing the program. e.g.

"""
Multi-Line
Comment
"""


# Operators: An operator is a symbol that performs a certain operation between operands.

# Types of Operators in Python: There are various types of operator in Python but we will discuss about the most commonly used ones.

# 1. Arithmetic Operators: These are operators which performs mathematical calculations like +, -, *, /, %, **
# e.g.

a = 5
b = 2

sum = a + b
print(sum)

diff = a - b
print(diff)

pro = a * b
print(pro)

div = b / a
# One interesting thing in Python is that result of division always produces Float value.
print(div)

mod = b % a
print(mod)

pow = a**b
print(pow)

# 2. Relational Operators: Also known as Comparison operators. These are used to compare two values and it always returns boolean as output like ==, !=, >, <, >=, <=
# e.g.

a = 50
b = 20

print(a == b)
print(a != b)
print(a > b)
print(a < b)
print(a >= b)
print(a <= b)

# 3. Assignment Operators: These are used assign values in a variable like =, +=, -=, *=, /=, %=, **=
# e.g.

num = 10
print("num: ", num)

num += 10  # Can also be written as num = num + 10
print("num: ", num)

num -= 10  # Can also be written as num = num - 10
print("num: ", num)

num *= 2  # Can also be written as num = num * 2
print("num: ", num)

num /= 2  # Can also be written as num = num / 2
print("num: ", num)

num %= 2  # Can also be written as num = num % 2
print("num: ", num)

num **= 2  # Can also be written as num = num ** 2
print("num: ", num)

# 4. Logical Operators: These are used to perform logical operations and always returns boolean as output like and, or, not
# e.g.

num1 = 50
num2 = 30

print(not False)
print(not (a > b))
# Here, a > b so, output will be true but 'not' will reverse the answer i.e. it will convert true into false and vice-versa.

val1 = True
val2 = False
print("and operator: ", val1 and val2)
# Returns true only if all the provided values are true else returns false.

print("and operator: ", (num1 == num2) and (num1 > num2))

print("or operator: ", val1 or val2)
# Returns true is either of the provided value is true else returns false.

print("and operator: ", (num1 == num2) or (num1 > num2))


# Type Conversion: This means converting one data type into another. There are two types:

1. Conversion: This happens automatically in Python. In Java this is known as Implicit conversion (Widening Casting). e.g. Let us consider we want to add to variables as shown below:
a = 2  # This is Integer value.
b = 4.25  # This is Float value.

sum = a + b
# Sum will automatically convert into Float value because Float is superior than Integer as it can store more information i.e. the decimal values too.
print(sum)

2. Casting: This is done manually by developers. In Java this is known as Explicit conversion (Narrow Casting). e.g. Let us consider we want to add to variables as shown below:

a = "2"  # This is String value.
b = 4.25  # This is Float value.

sum = a + b
# Sum will throw error as it cannot directly convert result into different data type so, we need to manually convert String into Integer or Float.
print(sum)

After Casting:
a = int("2")  # Converting String into Integer.
b = 4.25  # This is Float value.

print(type(a))
sum = a + b
print(sum)

OR, We can also convert it into Float value.
a = float("2")  # Converting Sting into Float.
print(type(a))

=> But if character were present instead of number inside a String then we can not do type casting, if we try then it will throw error as, characters can not be converted into Integer or Float.

Other e.g.

a = 3.14
a = str(a) # Converting Float into String

print(type(a))


# Taking input from user in Python: For this we use input(), this method is used to accept values using keyboard from user.
NOTE: Input from user is always a String value so you need to type cast this value as per your desired type. e.g.

input()  # By default it is String type.
int(input())  # Converting input into String.
float(input())  # Converting input into Float.
input("Enter your name: ") # Displaying message for input.

name = input("Enter your name: ") # Storing input into a variable
print("Welcome! ", name)

age = input("Enter your age: ")
print(type(age)) // It will show as String
print("Your age is", age)

=> This is where type casting comes into picture mostly.
val = int(input("Enter some value")) // Converting input into Integer.
print(type(val))

Other e.g.

name = input("Enter name: ")
age = int(input("Enter age: "))
marks = float(input("Enter marks: "))

print("Welcome,", name)
print("age=", age)
print("marks=", marks)


# Practice Questions:

1. Write a Program to input 2 numbers & print their sum.
Solution:

num1 = int(input("Enter num1: "))
num2 = int(input("Enter num2: "))

sum = num1 + num2
print("Sum =", sum)

2. Write a Program to input side of a square & print its area.
Solution:

side = float(input("Enter length: "))

area = side * side
print("Area of square =", area)

3. Write a Program to input 2 floating point numbers & print their average.
Solution:

num1 = float(input("Enter num1: "))
num2 = float(input("Enter num2: "))

print("Average =", (num1 + num2) / 2)

4. Write a Program to input 2 int numbers, a and b. Print True if a is grater than or equal to b. If not print False.
Solution:

a = int(input("Enter num1: "))
b = int(input("Enter num2: "))

print(a >= b)


# Strings: String is a data type that stores a sequence of characters. It can have one or more than one character or sometimes it can be an empty String. e.g.

str1 = "This is a string"
str2 = "Jain University"
str3 = """this is a string"""

=> Why do we have three different ways to define string in Python?
Because, if we want to use single apostrophe or double quotes then we can face issue. e.g.

str4 = 'this is Jain University's session' 
# For this one we cannot use single quotes as Python will get confused but we need to use double quotes.

=> Similarly, if we want to write something in double quotes then we can use single quotes. e.g.

str5 = '"Honda" make best cars in the world'

=> Similarly, if we want to write something in multiple sentences in a single string then that is not possible so, in order to achieve this we use "Escape sequence characters". These are some special characters that are used to formatting like next line, tabs, etc. this can be achieved by using \n. e.g.

str6 = "This is a string.\nWe are creating it in Python." 
# Here, \n is for next line. Similarly, for tab you can use \t.
print(str6)


# Different operations on String:

1. concatenation: This means combining two or more strings together. e.g.

str1 = "Jain"
str2 = "University"
print(str1 + str2)

# We can also store this in some variable and print it as shown below.
finalStr = str1 + str2
print(finalStr)

2. length(): By using length function we can find the size of the string. e.g.

str = "This is a string."
print(len(str))

=> Or, we can store it in some variable like shown below.
size = len(str)
print(size)


# Indexing: When a string is created in Python then each character in a string gets its index value i.e. the position of that particular character. Just like in all other programming language in Python also indexing starts with zero.

=> Why do we need indexing?
Because it helps us to access individual character's of a string. e.g.

str = "Jain University"
ch = str[0]
print(ch)
print(str[6])

Note: With the help of index you can only access characters but cannot modify it because Strings are immutable i.e. once created cannot be modified.


# Slicing: It means accessing specific part of a string. It can be done by: stringName[startingIndex : endingIndex] where endingIndex is exclusive. e.g.

str = "Jain University"
print(str[1:4])

=> If you want to access from startingIndex all the way up to the end of the string then you pass endingIndex + 1 or, you can also use len(str) instead of endingIndex or, also you can avoid passing endingIndex i.e. Python automatically assumes that you want to go all the way up to the end of the string. e.g. are shown below.

print(str[1:4])
print(str[5:15])
print(str[5 : len(str)])
print(str[5:])

=> Similarly, if you miss passing startingIndex then Python automatically assumes that you want to start from zeroth index. e.g.

print(str[:7])

=> Also, Python supports -ve index slicing which is not available in other programming languages. Basically -ve indexing means you want start counting from len(str) i.e. from backside of the string. e.g.

print(str[-3:-1])  # Here also endingIndex is exclusive.


# String Functions: There are lots of function present in Python for Strings other than length. Some of them are explained below:

str = "i AM a developer"

1. print(str.startswith("i")) # Returns true if the string starts with given sub-string.

2. print(str.endswith("er"))  # Returns true if the string ends with given sub-string.

3. print(str.capitalize())  # Capitalizes 1st character of the string. It does not make change in the original string instead it created a copy of it with modification.
print(str) # Proof that original string is not changed.

=> In case if you want to modify the original string itself then you can store the new string into the same variable. e.g.

str = str.capitalize() # Modifying original string
print(str)

4. print(str.replace("am", "was")) # This replace the old sub-string with new one.
# This also does not modify the original string as strings are immutable.
print(str)  # Proof that original string is unchanged.

# => You can also do something like:
print(str.replace("e", "o"))

5. print(str.find("am"))
# It searches for given sub-string and if found then returns 1st index of its occurrence and, if not found then returns -1.

print(str.find("z"))

=> In these methods don't think about -ve indexing of string as it works only with slicing, and not with these functions.

6. print(str.count("am")) # This counts occurrences of the given sub-string in the string and return the count.

print(str.count("a"))
print(str.count("z")) # If the given character or sub-string is not found then returns zero.

7. print(str.upper())  # Converts all characters of a string to upper case.

8. print(str.lower())  # Converts all characters of a string to lower case.


# Practice Questions:

1. Write a Program to input user's first name & print its length.
Solution:

firstName = input("Enter your first name: ")

print(len(firstName))

2. Write a Program to find the occurrence of '$' in a String.
Solution:

str = "I had $200. I gave $20 to Ravi. What is the remaining amount with me?"

print(str.count("$"))


# Conditional Statements:

=> For e.g. there are so lot of conditions like we can apply for driving license only if our age is 18 or above or on our marks which has grading like if our marks is 90 and above then we will get grade A and so on. All these conditions can also be be expressed in code too.

=> In Python to express the conditions we use:
1. if
2. if...else
3. elif

Syntax:
if (condition):
    statement1
elif (condition):
    statement2
else:
    statementN

e.g.

age = 21

if age >= 18:
    print("You can apply for license") 
    # Here you will get output as this statement but if I change the age value to 17 or smaller then there will be no output as we have not written else block.

e.g.

age = 16

if age >= 18:
    print("You can apply for license")
else:
    print("You are too your to drive")
    # This will be printed as output as the value of age is smaller than 18.

e.g. Lets take the example of traffic light in order to demonstrate elif.

light = "red"

if light == "red":
    print("STOP")
elif light == "green":
    print("GO")
elif light == "yellow":
    print("WAIT")

e.g. 

light = "..."

if light == "red":
    print("STOP")
elif light == "green":
    print("GO")
elif light == "yellow":
    print("WAIT")
else:
    print("Traffic light is not working.")
    # else is always written at the end of the if or elif as this block gets automatically executed if, none of above conditions are true.

=> In conditional the block which satisfied the condition only gets executed, other block does not comes into picture, we can verify that with below example: e.g.

light = "..."

if light == "red":
    print("STOP")
elif light == "green":
    print("GO")
elif light == "yellow":
    print("WAIT")
else:
    print("Traffic light is not working.")

print("End of code.")

=> We can write the same code as shown below: e.g.

if light == "red":
    print("STOP")
if light == "green":
    print("GO")
if light == "yellow":
    print("WAIT")

=> Then what is the difference between if and elif?
If we use if conditions then all if will get executed. Whereas, elif will come into picture only if, given if condition does not satisfy which brings us into conclusion that for writing elif we have to mandatorily write if. e.g.

num = 5

if num > 2:
    print("Number greater than 2")
if num > 3:
    print("Number is greater than 3")

# In the above example both statements are getting printed. But if we write the same code using elif then only 1st statement will get printed as elif gets checked only if, if condition is false.

num = 5

if num > 2:
    print("Number greater than 2")
elif num > 3:
    print("Number is greater than 3")

NOTE: whenever we are writing conditional then we have to give 4-space or 1-tab as 1-tab = 4-space, this things is know as indention in coding and Python strictly follows indention as we do not use { } in Python to separate blocks.

Other e.g.

Grade students based on marks:
marks >= 90, grade = "A"
90 > marks >= 80, grade = "B"
80 > marks >= 70, grade = "C"
70 > marks, grade = "D"

Solution:

# marks = 90
marks = int(input("Enter your marks: "))

if marks >= 90:
    # print("Grade A")
    grade = "A"
elif marks >= 80 and marks < 90:
    # print("Grade B")
    grade = "B"
elif marks >= 70 and marks < 80:
    # print("Grade C")
    grade = "C"
elif marks < 70:
    # print("Grade D")
    grade = "D"
else:
    print("Invalid marks")

print("Grade of the student -> ", grade)


# Nested Conditional: This means putting one condition under another condition.

=> Although nesting is not recommended, if we are nesting then also we should go more than 2-levels of nesting, as that makes code more complicate to understand and debug. In order to avoid nesting we can use logical operators.

e.g. for nesting:

age = 80

# Here we are trying to validate whether the age is 80 >= age >= 18.
if age >= 18:
    if age >= 80:
        print("You cannot drive")
    else:
        print("You can drive")
else:
    print("You cannot drive")

=> Avoiding nesting using logical operator.

age = 79

# Here we are trying to validate whether the age is 80 >= age >= 18.
if age >= 18 and age < 80:
    print("You can drive")
else:
    print("You cannot drive")


Practice Questions:

1. Write a Program to check if a number entered by the user is odd or even.

Solution:

num = int(input("Enter a number: "))

if num % 2 == 0:
    print("Even")
else:
    print("Odd")

2. Write a Program to find the greatest of 3 numbers entered by the user.

Solution:

num1 = int(input("Enter 1st number: "))
num2 = int(input("Enter 2nd number: "))
num3 = int(input("Enter 3rd number: "))

if num1 > num2 and num1 > num3:
    print("Num1 is greatest")
elif num2 > num1 and num2 > num3:
    print("Num2 is greater")
else:
    print("Num3 is greatest")

3. Write a Program to check if a number is multiple of 7 or not.

Solution:

num = int(input("Enter a number: "))

rem = num % 7
if rem == 0:
    print("Number is a multiple of 7")
else:
    print("Number is not multiple a of 7")


# Lists and Tuples in Python: If you have done programming in other programming languages then you must have used Arrays. The equivalent of Array in Python is Lists and Tuples.

1. Lists in Python: It is a built-in data type that stores set of values. It can store elements of different types i.e. we can store different data types in a single list whereas other program languages does not allow you to do this.

Lets say if we want to store marks of students in a class then we have to create variables equal to number of students in that class which will make coding very much time consuming and tracking those variables would also be very difficult. e.g.

marks1 = 45
marks2 = 65
marks3 = 54
marks4 = 78
marks5 = 69 # and so on.

=> In order to simplify this Python provides an in-built data type called as list where we can stored multiple values in a single variable as shown below:

marks = [45, 65, 54, 78, 69]

=> A list has it own syntax as you can see above and it has so many properties and methods which are similar to String properties and methods, also we can print list and check its type as shown below.

print(marks)
print(type(marks))

=> We can access individual element from a list as shown below:

print(marks[0])
print(marks[1])

=> Printing the size of the list.

print(len(marks))

=> Storing different types of values in the same list also if you try to access the index which is not present in the list then it will throw error "IndexError: List index out of range". e.g.

student = ["Om Prakash", 85, "Delhi"]

print(student)
print(student[0])
print(student[1])
print(student[2])
print(student[3]) # This line will throw error as 3rd index is not present in the list.

# As we have discussed above that Lists and Strings are similar, but these are not same. The major difference is that Stings in Python as immutable but Lists are mutable i.e. Lists values can be changed in place. e.g.

student = ["Om Prakash", 85, "Delhi"]

print(student)
print(student[0])
print(student[1])
print(student[2])

student[0] = "Chaurasia"
print(student)
print(student[0])

=> List Slicing: Similar to String we also have slicing in List which is similar to String Slicing and in list also ending index is not inclusive and it also supports -ve index just like String Slicing. e.g.

marks = [85, 94, 76, 63, 48]

print(marks[1:4])
print(marks[:4])
print(marks[1:])

# Slicing on behalf of -ve index:

print(marks[-3:-1])
print(marks[-3:5])


# Methods in Lists:

list = [2, 1, 3]

1. append(): It adds one element at the end of the list.
list.append(4) # At a time only one value can be appended.
print(list)

=> If you do print(list.append(4)) then it will return "None" as it does not return anything because it is making change in the original list but the same is not applicable for Strings because String is immutable.

2. sort(): It arranges list items in ascending order by default.
list.sort()
print(list)

# For sorting in descending order:
list.sort(reverse=True)

=> If you do print(list.sort()) then it will return "None", the reason is same as above.

=> Sorting can also be applied on characters or strings in the list. e.g.

list = ["lichi", "banana", "apple", "mango"]

print(list)
list.sort()
print(list)

e.g.

list = ["u", "e", "i", "o", "a"]

print(list)
list.sort()
print(list)

3. reverse(): It reverses the list. e.g.

list.reverse()
print(list)

4. insert(): It is used to insert a item at specific index in a list.
list.insert(3, "vowels") # Here also you can insert only one item at a time.
print(list)

5. remove(): It removes the first occurrence of the given item.e.g.

list.remove("a")
print(list)

6. pop(): It removes the item from the list at given index.
list.pop(3)
print(list)

7. copy(): It makes the shallow copy of the list.

print("List: ",list)
list2 = list.copy()
print("List 2: ", list2)

list2.append(44)
print("List 2: ", list2)
print("List: ", list)

=> Also, we can directly copy list as shown below but it is different from copy() method above. Find the difference and let me know.

print("List: ", list)
list2 = list
print("List 2: ", list2)

list2.append(44)
print("List 2: ", list2)
print("List: ", list)

=> count(), extend(), and clear() methods, discover by yourself.


2. Tuples in Python: It is a built-in data type that lets us create "immutable" sequence of values. Unlike Lists, and like String, Tuples can not be changed once created.

=> A Tuple has its on syntax like Lists, which is shown below. e.g.

tuple = (87, 64, 33, 95, 76)
print(tuple)
print(type(tuple))

=> Accessing elements from tuple is same as list.
print(tuple[0])
print(tuple[1])

tuple[0] = "vowels"  # This operation is not possible in Tuple as Tuples are immutable. It will throw error "TypeError"

